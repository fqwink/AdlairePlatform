# コントリビューション・ガイド — AdlairePlatform

> **最終更新**: 2026-03-10（Ver.1.4-pre）

## ライセンスに関する重要事項

AdlairePlatform は **Adlaire License Ver.2.0** に基づき公開されています。
これは **ソース参照可能（Source Available）** であり、**オープンソースではありません**。

- 改変・再配布・商用利用は禁止されています
- コントリビューション（プルリクエスト・パッチ）は、権利者の承認を条件に許可されます
- **提供されたコントリビューションの著作権は Adlaire Group に帰属します**

詳細は [docs/Licenses/LICENSE_Ver.2.0.md](docs/Licenses/LICENSE_Ver.2.0.md) を参照してください。

---

## コントリビューションの流れ

### 1. Issue の作成

バグ報告・機能提案は、まず GitHub Issue を作成してください。

**バグ報告に含める情報:**
- 再現手順
- 期待される動作と実際の動作
- PHP バージョン・Web サーバー・OS
- 該当するエラーメッセージやログ

**機能提案に含める情報:**
- 提案の概要と目的
- 想定されるユースケース

### 2. プルリクエストの作成

1. リポジトリをフォーク
2. フィーチャーブランチを作成（`feature/説明` または `fix/説明`）
3. 変更を実装
4. プルリクエストを作成し、変更内容を説明

**プルリクエストのルール:**
- 1 つの PR につき 1 つの目的（バグ修正 or 機能追加）
- 既存のコードスタイルに従う
- 既存機能を壊さないことを確認する

### 3. レビューとマージ

- 権利者がレビューを行い、承認後にマージします
- 修正を求められた場合は、同じブランチに追加コミットしてください

---

## 開発環境のセットアップ

### 必要条件

- **PHP 8.2 以上**（必須）
- **Web サーバー**: Apache（mod_rewrite 有効）または PHP ビルトインサーバー

### ローカル起動

```bash
# PHP ビルトインサーバーで起動
php -S localhost:8000

# ブラウザで http://localhost:8000 にアクセス
```

### ディレクトリ構造

詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) を参照してください。

---

## コーディング規約

### PHP

- **外部依存なし** — Composer パッケージを追加しない
- **PHP 8.2+ 対応** — PHP 8.2 以降の機能を使用可能
- **関数・メソッド名**: `camelCase`
- **クラス名**: `PascalCase`
- **エンジンパターン**: 新機能は `engines/` に独立したエンジンクラスとして追加
- **セキュリティ優先**: 入力バリデーション・出力エスケープ・CSRF 保護を徹底
- **グローバル変数の直接使用を避ける** — `AppContext` を経由する

### JavaScript

- **バニラ JS**（フレームワーク不使用）
- **ES2020+** 構文
- **モジュールシステム未使用** — `import`/`export` は使わない

### 全般

- インデントはスペース 4 つ
- 不要なコメントを追加しない — コードが自己説明的であること
- 既存のパターンに合わせる

---

## セキュリティに関するガイドライン

セキュリティ脆弱性を発見した場合は、**公開 Issue を作成しないでください**。
代わりに、権利者に直接連絡してください。

コントリビューション時に注意すべきセキュリティ項目:
- ユーザー入力は必ずバリデーション・サニタイズする
- HTML 出力には `h()` 関数でエスケープする
- 状態変更を伴うエンドポイントには CSRF トークン検証を含める
- ファイルパス操作にはパストラバーサル対策を施す
- メールヘッダーには `MailerEngine::sanitizeHeader()` を使用する

---

## ドキュメントの更新

コードの変更に伴い、以下のドキュメントも必要に応じて更新してください:

| ドキュメント | 更新が必要なケース |
|---|---|
| `RELEASE-NOTES.md` | すべての変更 |
| `README.md` | エンジン追加 |
| `docs/ARCHITECTURE.md` | アーキテクチャ・ファイル構成の変更 |
| `docs/AdlairePlatform_Design.md` | API・テンプレート構文・設計仕様の変更 |
| `docs/features.md` | 機能追加・変更 |

---

## 質問・相談

不明点がある場合は、GitHub Issue で質問してください。
